#pragma once
#include "primitives/vector.h"

static const float PI = 3.14159f;
static const float TWO_PI = 2.0f * PI;
static const float FOUR_PI = 4.0f * PI;
static const float PI_SQRD = PI * PI;
static const float INV_PI = 1.0f / PI;
static const float INV_TWO_PI = 1.0f / TWO_PI;
static const float INV_FOUR_PI = 1.0f / FOUR_PI;
static const float INV_PI_SQRD = INV_PI * INV_PI;
static const float PI_OVER_TWO = PI / 2.0f;
static const float PI_OVER_THREE = PI / 3.0f;
static const float PI_OVER_FOUR = PI / 4.0f;
static const float PI_OVER_SIX = PI / 6.0f;
static const float ERROR = 0.0005f;
static const float TWO_ERROR = ERROR * 2.0f;
static const float FOUR_ERROR = ERROR * 4.0f;
static const float EIGHT_ERROR = ERROR * 8.0f;
static const float SIXTEEN_ERROR = ERROR * 16.0f;

static const Vector3 ZERO_VECTOR(0.0f);
static const Vector3 ONE_VECTOR(1.0f);
static const Vector3 TO_Y(0.2126f, 0.7252f, 0.0722f);
